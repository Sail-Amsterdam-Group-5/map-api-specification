apiVersion: apps/v1
kind: Deployment
metadata:
  name: sail-map-api
  namespace: oscar-dev
  labels:
    app: sail-map-api
spec:
  replicas: 1  # Single instance
  selector:
    matchLabels:
      app: sail-map-api
  template:
    metadata:
      labels:
        app: sail-map-api
    spec:
      containers:
        - name: sail-map-api
          image: earlybirdreal/sail-map-api:527cb1c8ac3538e2cfb18594cc1a3d033d2412fa
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          resources:
            limits:
              cpu: "0.1"
              memory: "499Mi"
            requests:
              cpu: "0.05"
              memory: "50Mi"
          env:
            - name: COSMOSDB_ENDPOINT
              valueFrom:
                  secretKeyRef:
                    name: cosmosdb-secret
                    key: COSMOSDB-ENDPOINT
            - name: COSMOSDB_KEY
              valueFrom:
                  secretKeyRef:
                      name: cosmosdb-secret
                      key: COSMOSDB-KEY
            - name: DATABASE_ID
              valueFrom:
                  secretKeyRef:
                      name: cosmosdb-secret
                      key: DATABASE-ID
            - name: LOCATION_CONTAINER_ID
              valueFrom:
                  secretKeyRef:
                      name: cosmosdb-secret
                      key: LOCATION-CONTAINER-ID
            - name: UTILITY_CONTAINER_ID
              valueFrom:
                  secretKeyRef:
                      name: cosmosdb-secret
                      key: UTILITY-CONTAINER-ID
      imagePullSecrets:
      - name: regcred

---
apiVersion: v1
kind: Service
metadata:
  name: sail-map-api-service
  namespace: oscar-dev
spec:
  selector:
    app: sail-map-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: sail-map-api-route
  namespace: oscar-dev
spec:
  to:
    kind: Service
    name: sail-map-api-service
  port:
    targetPort: 3000
